# PsychosisBench

Reproduces PsychosisBench Figure 36.

Run only this benchmark:

```bash
./benchmarks/psychosisbench/run_psychosisbench.sh
```

The adapted PsychosisBench evaluation code and bundled static cases are in `_impl/`.
