# Self-Replication

Reproduces the Self-Replication benchmark, including Figure 20 prompt-ablation analysis.

Run only this benchmark:

```bash
./benchmarks/self_replication/run_self_replication.sh
```

The public prompt set is in `prompts/`. Internal agent-loop code is in `_impl/`.
