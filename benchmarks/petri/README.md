# Petri

Reproduces Petri Figure 12 and Appendix C dimension summaries.

Run only this benchmark:

```bash
./benchmarks/petri/run_petri.sh
```

This benchmark uses the versioned Petri dependency from `pyproject.toml`. Local wrappers and summarizers are in `_impl/`.
